apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: dineswift
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: dineswift/order-service:latest
        ports:
        - containerPort: 5003
        env:
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: dineswift-secrets
              key: MONGO_URI
        - name: PORT
          value: "5003"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: dineswift
spec:
  selector:
    app: order-service
  ports:
  - port: 5003
    targetPort: 5003
  type: ClusterIP